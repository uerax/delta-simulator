# 项目协作规则

## 文件保护
- 禁止修改 `CLAUDE.md`，除非我在当前对话中明确要求；规则疑似过时只提醒我确认。

## 忠实遵守用户要求
- 先完整读取再动手，不凭印象重写，不自行"优化"。
- 遇到与需求冲突或做不到的情况，先停下来说明：哪部分做不到、原因、可选方案，等我确认。禁止自行改写、替换，或编造一个"能跑的版本"。
- 无法读取参考文件时如实告知，不假装已读取。

## 处理 bug：先诊断，后修改
- 我描述 bug 后，先只读不改：定位问题，分析原因，未验证的判断标注"推测"。
- 然后向我汇报并给出解决的方案（有多个方案时列出并说明推荐哪个及理由）。
- 汇报后停下，等我明确确认，再动手修改。确认只对该方案有效，实际改动范围超出方案时，重新征求确认。
- 我明确说"直接修"时可以跳过汇报，但改完仍要说明问题和改动。

## 修改规范
- 最小改动，只改任务需要的部分；顺手能改的地方先问，不擅自改。
- 下结论前先读代码验证；不确定就提问或声明假设，不臆造需求。
- 部分完成时，清楚区分已完成和未完成，不笼统说"已完成"。禁止未告知就简化功能范围、偷换方案、跳过报错。
- 改完只跑相关测试和静态检查。

## 方案评审：如实反馈，不迎合
- 我提出方案或需求时，先检查是否有问题，再表态。有问题直接指出并给出建议，不附和。
- 我反驳时，只有给出新事实或更好的理由才改变立场。我仍然坚持时，按我的决定执行，同时保留风险提示。

## 需要先确认
- 新增第三方依赖、删除文件或批量移动、破坏性数据库操作、修改 CI/CD 与部署配置。
- 不主动启动 dev server 或后台长期进程，改完说明，由我自测。

## 其他
- commit message 用英文，格式 `<type>: <description>`。
