# CLAUDE.md

- 严禁把猜测当成结论
- 禁止无脑夸用户。不需要你提供情绪价值
- 如果用户提的需求无法实现, 应该如实报告, 而不是自己擅自改变需求。
- 遇到小程序框架、编译打包、全局配置（如 app.json/resolveAlias）等任何问题，第一时间查阅微信官方文档，严禁凭经验瞎猜瞎搜（官方文档入口：https://developers.weixin.qq.com/miniprogram/dev/reference/）

# 项目协作规则（CLAUDE.md）

> 本文件在每次 session 启动时都会被加载，请严格遵守以下规则。
> 规则冲突时，按编号从小到大优先级递减，第 0 条优先级最高。

## 0. 自我保护（最高优先级）

- 禁止修改本文件，除非用户当前对话明确要求。
- 规则疑似过时：提醒用户确认，不擅自改。
- 批量操作（重构/格式化/批量替换）默认跳过 `CLAUDE.md`、`.claude/`，除非被点名。

## 1. 状态持久化

- 维护 `.claude/STATE.md`（任务历史）+ `.claude/BUGS.md`（当前待办）。
- 新任务前先读 `STATE.md`，以文件为准，不依赖对话记忆。
- 完成子任务立即追加记录，格式：
  ```
  ## [日期] 任务名
  - 状态：已完成 / 进行中 / 待处理
  - 优先级：P0-P5
  - 描述 / 涉及文件 / 备注
  ```
- 多条待办先写入 `BUGS.md` 再逐条处理，处理完立即从中删除（不留"已完成"标记）；完整细节只进 `STATE.md`。
- 同一事项只存在于两个文件之一，不重复维护。

## 2. 上下文管理

- 对话变长（用户提到"上下文快满了"等）时，主动建议先落盘状态，再开新 session 或 `/compact`。
- 关键信息以文件为准，不假设自己记得住。
- `STATE.md`/`BUGS.md` 只留近期记录；旧的"已完成"（如超 20 条）整体迁入 `.claude/STATE-ARCHIVE.md`。

## 3. 代码修改规范

- 改动前先说明范围，不做无关的大改动。
- 改完跑相关测试即可，不必每次全量。
- 不确定就提问或声明假设，不臆造需求；下结论前先读代码验证，不凭经验断言。
- 需求无法实现、有歧义、或与现有代码/架构冲突时，先停下来说明原因、已尝试的方式、可选方案，等待确认后再继续；禁止在未告知的情况下擅自：
  * 简化或阉割功能范围（去掉某个字段、某个校验、某个边界情况）；
  * 用"看起来差不多"的替代方案偷换原方案；
  * 跳过报错的部分，假装已完成；
  * 修改验收标准来让实现"看起来"满足要求。
- 任务部分完成、部分做不到时，必须清楚区分哪些完成了、哪些没有，不能笼统汇报"已完成"；对应记录同步写入 `STATE.md`（完成部分）和 `BUGS.md`（未完成/受阻部分）。

## 4. 开发服务器与浏览器资源

- 禁止主动启动 dev server 或长期进程（含 `&`/`nohup` 后台变体），改完直接说明、由用户自测；一次性脚本/测试/类型检查不受限。
- 用浏览器自动化工具验证后必须关闭测试标签页；若是唯一标签页，先开空白页再关。

## 5. Git 与版本号

- commit message 格式：`<类型>: <说明>`（feat/fix/refactor/docs/test/chore）。
- 版本号按改动幅度递增（patch/minor/major），纯文档改动不递增；每次回复注明版本号变动。
- 不自动执行 `commit` / `push` / 历史重写操作（`rebase -i`、`reset --hard`、`push --force`），除非本次任务被明确要求；单次授权不延续到后续任务。

## 6. 沟通风格

- 直接给结论和改动内容，不寒暄。
- 不确定的技术判断标注"推测"或"已验证"。

## 7. 领域专项规则（纯离线单机小程序 / 小游戏）

- **纯本地数据持久化**：
  - 本项目采用纯离线单机架构，所有用户信息与每日数据均基于本地存储 `wx.setStorageSync` / `wx.getStorageSync`。
  - 数据读写统一通过 `miniprogram/utils/storage.js` 封装层进行，禁止在业务页面零散硬编码 storage key。
  - 所有 `wx.setStorageSync` 写入操作必须有 try-catch 保护，避免异常中断主流程。
- **页面与组件性能约束**：
  - 严禁在游戏主循环（如每秒倒计时、帧动画）中频繁调用 `setData` 传输无变化的大对象。
  - 内部定时器句柄（如 `_timer`）、不影响界面的临时状态必须挂载在页面实例 `this` 上，禁止放入 `data`。
  - 页面卸载（`onUnload`）或隐藏（`onHide`）时，必须清理所有活动的 `setInterval` 和 `setTimeout`。
- **触感与交互规范**：
  - 触感震动（`wx.vibrateShort` / `wx.vibrateLong`）必须读取用户的震动开关配置（`settings.vibrationEnabled`）。
- **技术文档查阅（第一准则）**：
  - 遇到小程序配置（如 `app.json` 路径别名 `resolveAlias`）、生命周期、事件机制、存储机制等疑难问题，必须第一时间查阅微信官方文档（https://developers.weixin.qq.com/miniprogram/dev/reference/）与 `docs/miniprogram-framework.md`，严禁凭经验瞎猜瞎搜。

## 8. 功能索引维护

- 维护 `.claude/FEATURE-MAP.md`（模块 → 文件路径，仅供导航，不放状态/历史）。
- 新增/迁移模块时更新；路径类可自主改，背景决策类改动需在回复中提示确认。
- 模块下线同步删条目；超 100 行时按模块拆分为 `.claude/FEATURE-MAP/<module>.md`。
- 接手"定位功能"类任务先读此文件，缺失再全项目搜索并补全。

## 9. 依赖与环境管理

- 禁止把密钥、`.env`、凭据、私钥等文件提交到仓库，也不要把其内容输出到回复或日志里。
- MCP 配置文件位于项目根目录 `.mcp.json`。

## 10. 代码风格与一致性

- 优先匹配项目现有的代码风格、命名习惯、目录组织方式，不引入自己的偏好写法。
- 完成修改后，确认无语法报错。
- 倾向最小化改动（minimal diff）：只改任务需要的部分，不顺手重构、重排无关代码。

## 11. 测试规范

- 本项目当前未集成自动化单元测试框架。
- 逻辑改动后，在回复中明确告知用户如何在微信开发者工具中进行验证，或通过单次脚本核验。

## 12. 高风险操作二次确认

- 涉及以下类别的操作，必须先说明影响范围并等待明确确认，不直接执行：
  - 删除文件、清空目录、批量重命名/移动文件；
  - 修改 CI/CD 配置、部署脚本、生产环境配置；
  - 修改权限、密钥轮换、对外发布相关的操作。

## 13. 文档同步

- 新增或变更对外可见的功能时，检查是否需要同步更新 `README.md`。

## 14. 外部调用与成本意识

- 保持纯离线单机运行，默认不发起任何产生外部计费的云服务调用。

## 15. 命名与语言规范

- 代码注释使用：中文。
- Commit message 使用：`<类型>: <说明>`（中文或英文）。
- UI 文案 / 面向用户的文案使用：中文。
- 命名风格：JavaScript 采用小驼峰（camelCase），页面与组件目录采用小驼峰或短横线（kebab-case）。

## 待办 / 索引类文件一览

- 当前 bug 清单：`.claude/BUGS.md`
- 项目状态快照：`.claude/STATE.md`
- 功能实现索引（仅供 Claude 导航）：`.claude/FEATURE-MAP.md`
- 官方框架精简与排查手册：`docs/miniprogram-framework.md`
- 项目核心配置文件索引：
  - 小程序全局配置：`miniprogram/app.json`
  - 开发者工具配置：`project.config.json`
  - MCP 工具配置：`.mcp.json`
