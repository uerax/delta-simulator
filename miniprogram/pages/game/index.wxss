/* pages/game/index.wxss */
page {
  background-color: #1e1e2e;
  color: #cdd6f4;
  height: 100vh;
  box-sizing: border-box;
}

.game-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx 32rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

/* 顶部状态栏 */
.status-bar {
  width: 100%;
  display: flex;
  justify-content: space-around;
  background: #313244;
  border-radius: 20rpx;
  padding: 24rpx 16rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.3);
  margin-bottom: 50rpx;
}

.status-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.status-item .label {
  font-size: 24rpx;
  color: #a6adc8;
  margin-bottom: 8rpx;
}

.status-item .value {
  font-size: 40rpx;
  font-weight: bold;
}

.time-value {
  color: #89b4fa;
}

.time-value.danger {
  color: #f38ba8;
  animation: pulse 0.5s infinite alternate;
}

.score-value {
  color: #a6e3a1;
}

.combo-value {
  color: #f9e2af;
}

/* 九宫格棋盘 */
.grid-board {
  width: 620rpx;
  height: 620rpx;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: repeat(3, 1fr);
  gap: 24rpx;
  margin-bottom: 40rpx;
}

.grid-cell {
  background: #313244;
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 64rpx;
  transition: all 0.15s ease-out;
  box-shadow: inset 0 2rpx 6rpx rgba(255, 255, 255, 0.05), 0 8rpx 16rpx rgba(0, 0, 0, 0.2);
}

.grid-cell:active {
  transform: scale(0.95);
}

.grid-cell.cell-active {
  background: linear-gradient(135deg, #f38ba8, #fab387);
  box-shadow: 0 0 30rpx rgba(243, 139, 168, 0.6);
  transform: scale(1.02);
}

.grid-cell.cell-wrong {
  background: #585b70;
  animation: shake 0.3s ease-in-out;
}

.active-pulse {
  animation: popIn 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

/* 底部提示 */
.footer-tips {
  font-size: 28rpx;
  color: #a6adc8;
  margin-top: 20rpx;
  height: 48rpx;
}

.action-btn-area {
  margin-top: 40rpx;
  width: 100%;
}

.start-btn {
  background: linear-gradient(135deg, #89b4fa, #b4befe);
  color: #11111b;
  font-weight: bold;
  font-size: 34rpx;
  border-radius: 48rpx;
  padding: 24rpx 0;
  box-shadow: 0 8rpx 24rpx rgba(137, 180, 250, 0.4);
}

/* 弹窗样式 */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}

.result-modal {
  width: 600rpx;
  background: #1e1e2e;
  border: 2rpx solid #45475a;
  border-radius: 32rpx;
  padding: 48rpx 36rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: 0 20rpx 50rpx rgba(0, 0, 0, 0.5);
  animation: popIn 0.3s ease-out;
}

.modal-title {
  font-size: 40rpx;
  font-weight: bold;
  color: #cdd6f4;
  margin-bottom: 24rpx;
}

.new-record-badge {
  background: #f9e2af;
  color: #11111b;
  font-size: 26rpx;
  font-weight: bold;
  padding: 8rpx 24rpx;
  border-radius: 30rpx;
  margin-bottom: 24rpx;
}

.result-list {
  width: 100%;
  background: #313244;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 36rpx;
  box-sizing: border-box;
}

.result-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 0;
  font-size: 28rpx;
}

.row-label {
  color: #a6adc8;
}

.row-value {
  color: #cdd6f4;
  font-weight: bold;
}

.score-highlight {
  font-size: 36rpx;
  color: #fab387;
}

.modal-actions {
  width: 100%;
  display: flex;
  gap: 20rpx;
}

.modal-btn {
  flex: 1;
  font-size: 30rpx;
  font-weight: bold;
  border-radius: 36rpx;
  padding: 20rpx 0;
}

.play-again-btn {
  background: #89b4fa;
  color: #11111b;
}

.back-home-btn {
  background: #45475a;
  color: #cdd6f4;
}

@keyframes pulse {
  from { transform: scale(1); }
  to { transform: scale(1.1); }
}

@keyframes popIn {
  from { transform: scale(0.8); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-10rpx); }
  75% { transform: translateX(10rpx); }
}
